# Core dependencies
python-dotenv
numpy>=1.24.0

# Install custom livekit from local directory (will be installed via Dockerfile)
# livekit and livekit-agents are installed from ./livekit-agents/

# Plugin dependencies for custom livekit-agents
openai>=1.0.0
deepgram-sdk
cartesia